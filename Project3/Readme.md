
### Setup

```{sh}
# Setup venv
python3 -m venv virtualenv
source virtualenv/bin/activate

# Install all libraries
pip install jupyter --user
pip3 install --upgrade tensorflow
pip3 install pandas numpy keras scikit-learn matplotlib
```